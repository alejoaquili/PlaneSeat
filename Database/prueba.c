#include <stdio.h>
#include "planeSeatDBHandler.h"
/*
int main(void)
{
	dataBaseADT db = createPlaneSeatDataBaseHandler();
	
	if(db == NULL)
		printf("ASDASD\n");

	addNewFlight(db, "ABC", "AB", "CD");
	//addNewFlight(db, "ABD", "AC", "CE");
	int qty = 0;
	char * q = "SELECT * FROM flightSeats;";
	//
	addNewReservation(db, "ABC", 'a', 1, "1234");
	executeQueryDataBase(db, q, true);
	deleteReservation(db, "ABC", "1234", 'a', 1);
	printf("1\n\n\n\n\n\n");
	executeQueryDataBase(db, q, true);
	//flight_t * f = getFlights(db, &qty);

	/*for(int i = 0 ; i < qty ; i++)
		printf("Flight:%s, %s, %s\n",f[i].flightNumber, f[i].origin, f[i].destination);
	/*flightSeat_t * fsd = getFlightSeatsDistribution(db, "ABC", &qty);
	


	for(int i = 0; i < qty ; i ++)
	{
		printf("FlightSeat: %s, %c, %d, %d\n",fsd[i].flightNumber, fsd[i].colLetter, fsd[i].rowNumber, fsd[i].occupied);
	}

	deleteDataBase(db, true);

}
*/
